#include<stdio.h>
#include<math.h>
int main(){
	int p,base,result;
	printf("\n enter the value of pow");
	scanf("%d",&p);
	printf("\n enter the value of base");
	scanf("%d",&base);
	result=pow(base,p);
	printf("power=%d",result);
	return 0;
}
	
	
